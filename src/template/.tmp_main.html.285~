<!DOCTYPE HTML>
<html>
    <head>
        <title>Magic Mirror</title>
    </head>
    <body>
        <h1>Question</h1>
        <p>
            {{question.text}}
        </p>
        {% for option in options %}
        <ol>
            {{option.text}}
        </ol>
        {% endfor %}
    </body>
</html>
